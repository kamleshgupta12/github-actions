name: Validate files
on: push
jobs:
    validate-file:
        runs-on: Ubuntu-latest

        steps:
        - name: get-repo-code
          user: actions/checkout@v4

        - name: validate html file
          user: test -f index.html

        - name: validate js file
          user: test -f script.js